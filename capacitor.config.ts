import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'finnazz_web-APP',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
